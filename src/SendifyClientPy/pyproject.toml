[project]
name = "sendify-client"
version = "0.1.0"
description = "Python client for Sendify REST API"
requires-python = ">=3.9"
dependencies = ["requests>=2.31.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
